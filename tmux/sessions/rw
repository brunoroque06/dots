#!/usr/bin/env bash

SESSION='rw'

tmux new-session -s $SESSION -n 'dotfiles' -d
tmux send-keys "cd $HOME/Projects/dotfiles" C-m

tmux new-window -t $SESSION:2 -n 'sulfur'
tmux send-keys "cd $HOME/Projects/raccoon-works/Sulfur" C-m

tmux attach-session -t $SESSION
