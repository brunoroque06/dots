color cursor black yellow

bind main F !?git push --force
bind main P !git push

bind refs B ?git branch "%(prompt Enter new branch name: )"

bind status A !git add --all
bind status F !git commit --amend

